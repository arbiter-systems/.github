name: Sync Blocked By

on:
  workflow_dispatch:

concurrency:
  group: sync-blocked-by
  cancel-in-progress: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Disabled
        run: echo "Sync Blocked By is disabled because the script is not trusted and should not consume scheduled GitHub Actions minutes."
