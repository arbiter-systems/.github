name: Add to Arbiter Project

on:
  workflow_dispatch:

concurrency:
  group: add-to-arbiter-project
  cancel-in-progress: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Disabled
        run: echo "Add to Arbiter Project is disabled to prevent issue and PR event automation from consuming GitHub Actions minutes."
